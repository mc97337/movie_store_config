server:
  servlet:
    context-path: /api/poli
  port: 9081
spring:
  application:
    name: users
  profiles:
    active: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9010/eureka/
